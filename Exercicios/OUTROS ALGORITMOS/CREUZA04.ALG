Algoritmo "creuza04"
Var   a, b, c, d: real

Inicio
      escreval("Qual o valor do emprestimo? ")
      leia(a)
      b <- a * 1.2
      escreval("Em quantas parcelas? ")
      leia(c)
      d <- b / c
      escreva("Vou pagar ", c," parcelas de ", d, " reais")

Fimalgoritmo