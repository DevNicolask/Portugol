algoritmo "ParOuImparprocedimento"
var x: inteiro

// Procedimento pra analizar se o resto de A por 2 � igual a 0 ou n�o.
Procedimento Teste(A: inteiro)
inicio
   Se (A % 2 = 0) entao
      Escreval("O n�mero ", A, " � PAR!")
   Senao
      Escreva("O n�mero ", a, " � IMPAR!")
   FimSe
FimProcedimento

inicio

// Pede ao usu�rio que digite um n�mero.
   Escreva("Digite um n�mero: ")
   Leia(X)
   
// Chama o procedimento que testa se o n�mero � par e passa o n�mero 'X' como pa�metro para 'A'.
   Teste(x)
fimalgoritmo