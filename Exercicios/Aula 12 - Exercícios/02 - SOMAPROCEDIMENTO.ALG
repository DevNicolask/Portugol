algoritmo "SomaProcedimento"
var
 X, Y: Inteiro
 
// Chama uma fun��o que realiza a soma de A e B e mostra isso na tela.
Procedimento Soma(A, B: Inteiro)
inicio
   EscrevaL("Recebi o valor ",A)
   Escreval("Recebi o valor ",B)
   Escreval("A soma entre os dois e ", A + B)
FimProcedimento

inicio

// Define o valor de X e de Y.
    X <- 5
    Y <- 3
    
// Chama o procedimento de Soma e passa X e Y como par�metros para A e B.
    Soma(X, Y)
fimalgoritmo
